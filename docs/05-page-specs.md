# 05 — 页面规格

> 共 16 个页面。每个页面按 8 个维度描述：页面目标、入口、主要内容、主要操作、状态变化、错误状态、空状态、无障碍要求。
> Cloud contract note: API calls use `POST /api/auth/send-code`, `POST /api/auth/verify-code`,
> `POST /api/user/role`, and the canonical order statuses from `AGENTS.md` / the backend repository's `docs/api_spec.yaml`.
> Lower-case status names in older page examples are historical aliases only.

---

## 页面 1：登录页（手机号 + 验证码）

**页面目标**：用户通过手机号 + 验证码登录，首次登录自动创建账号。

**入口**：App 启动（无有效 token 时）。

**主要内容**：
- App Logo / 名称"助盲跑"
- 手机号输入框（11 位数字键盘，placeholder: "请输入手机号"）
- "获取验证码"按钮（手机号格式正确后启用）
- 验证码输入框（6 位，placeholder: "请输入验证码"）
- 验证码倒计时（60 秒，"重新发送"链接）
- 环境切换入口（底部或角落，不显眼：Mock / 本地 / 生产）

**主要操作**：
- 输入手机号 → 点击"获取验证码"
- 输入验证码 → 自动验证（6 位填完后自动提交，或手动点击"登录"）
- 点击"重新发送"（倒计时结束后显示）
- 点击环境切换

**状态变化**：
- 手机号格式校验中 → 实时提示格式错误
- "获取验证码"点击 → 页面提示"验证码已发送"，按钮变为倒计时（60 秒）
- 验证码输入 → 调用 `POST /api/auth/verify-code` 并跳转
- 首次登录 → 角色选择页
- 非首次登录 → 直接进入上次选择的角色首页

**错误状态**：
- 手机号格式错误 → 红框 + "请输入正确的手机号"
- 验证码错误 → 抖动 + "验证码错误，请重新输入"
- 网络错误 → Toast "网络错误，请重试"

**空状态**：无（登录页必然有输入框和按钮）。

**无障碍要求**：
- 手机号输入框：accessibilityLabel = "请输入11位手机号"
- 验证码输入框：accessibilityLabel = "请输入6位验证码"
- "获取验证码"按钮：accessibilityHint = "点击后发送验证码"
- TTS：进入页面不自动播报（避免打扰）

---

## 页面 2：角色选择页

**页面目标**：用户选择本次使用的角色（盲人跑者 / 志愿者）。

**入口**：首次登录验证码验证成功后。
（~~用户从设置主动切换角色~~ —— **【未决 / 未实现】入口已删除**：后端没有角色切换端点，设置页的切换角色入口已从 App 中移除。见 `docs/04-user-flows-and-state-machine.md` 第 6 节。）

**主要内容**：
- 标题："请选择您的角色"
- 两个大型卡片：
  - 盲人跑者卡片：图标、标题"我是盲人跑者"、副标题"预约志愿者陪我跑步"
  - 志愿者卡片：图标、标题"我是志愿者"、副标题"陪伴盲人跑者完成跑步"
- 底部提示："一个手机号可同时拥有两个身份，后续可在设置中切换"

**主要操作**：
- 点击"盲人跑者"卡片 → 进入盲人资料页（首次）或盲人首页
- 点击"志愿者"卡片 → 进入志愿者认证页（首次）或志愿者首页

**状态变化**：
- 选择后保存 activeRole 到后端和本地
- 如果有角色对应的活跃订单（PENDING_ACCEPT / DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS），先弹窗拦截再决定

**错误状态**：
- 网络错误 → Toast "网络错误，请重试"
- 角色保存失败 → Toast "角色设置失败"

**空状态**：无。

**无障碍要求**：
- 每个角色卡片是单个 VoiceOver 元素，包含标题和副标题
- 盲人跑者卡片：accessibilityLabel = "我是盲人跑者，预约志愿者陪我跑步"
- 志愿者卡片：accessibilityLabel = "我是志愿者，陪伴盲人跑者完成跑步"

---

## 页面 3：盲人资料页

**页面目标**：盲人跑者填写个人资料，完成注册。紧急联系人已从本页拆出，由「页面 3b：紧急联系人管理页」维护。

**入口**：角色选择页选择"盲人跑者"（首次）/ 设置页 → 编辑资料。

**主要内容**：
- 标题："完善信息"
- 昵称输入框（必填）
- 跑步经验选择器（选填：无经验 / 偶尔跑 / 经常跑）
- 实名认证入口与状态（**下单硬门槛**，未通过时无法创建预约；状态取自 `BlindProfileResponse.verifyStatus`，仅 `NOT_VERIFIED` / `VERIFIED` / `FAILED` 三态，不存在「审核中」）
- "完成"按钮

**主要操作**：
- 填写所有必填项 → 点击"完成" → 保存到后端 → 首次注册继续引导至紧急联系人管理页，再进入盲人首页
- 选填跑步经验
- 已有资料时：预填已有数据

**状态变化**：
- 提交 → loading → 成功（跳转首页）
- 编辑模式（来自设置）：标题变为"编辑资料"，按钮文字变为"保存"

**错误状态**：
- 必填项为空 → 按钮禁用 + 空字段红框标记
- 手机号格式错误 → "请输入正确的手机号"
- 网络错误 → "保存失败，请重试"

**空状态**：无（首次使用时字段为空）。

**无障碍要求**：
- 每个输入框有清晰的 accessibilityLabel
- 必填字段标记为 accessibilityHint = "必填"
- 完成按钮：最小高度 64pt
- TTS：进入页面播报"请填写个人资料"

---

## 页面 3b：紧急联系人管理页

**页面目标**：维护 1～5 个紧急联系人，并保证恰好 1 个主联系人。**这是盲人下单的硬前置条件**。

**入口**：盲人资料页完成后（首次注册）/ 设置页 → 紧急联系人 / 创建预约被阻断时的跳转入口。

**主要内容**：
- 标题："紧急联系人"
- 联系人列表（每行：姓名、关系（选填）、脱敏电话、是否主联系人）
- "添加联系人"按钮（已有 5 个时禁用）
- 每行操作："编辑"、"设为主联系人"（非主联系人才显示）、"删除"

**新增 / 编辑表单**：
- 姓名输入框（新增必填）
- 电话输入框（新增必填，11 位数字键盘）
- 关系输入框（选填）
- 编辑时电话默认以脱敏形式展示；未修改则不提交该字段，服务端保留原值（后端 PUT 为 PATCH 语义）

**主要操作**：
- 新增 → `POST /api/users/{userId}/emergency-contacts` → 刷新完整列表
- 编辑 → `PUT /api/users/{userId}/emergency-contacts/{contactId}` → 刷新完整列表
- 删除 → `DELETE /api/users/{userId}/emergency-contacts/{contactId}` → 二次确认 → 刷新完整列表
- 设为主联系人 → `PUT /api/users/{userId}/emergency-contacts/{contactId}/set-primary` → 刷新完整列表

**状态变化**：
- 首个联系人由后端自动设为主联系人
- 设为主联系人为原子操作，原主联系人自动取消

**错误状态**：
- 已有 5 个 → 新增入口禁用，提示并播报"最多添加 5 个紧急联系人"
- 只剩 1 个时删除 → 阻止，提示"至少保留 1 个紧急联系人"
- 尝试删除主联系人（存在其他联系人时）→ 要求先把另一个设为主联系人
- 手机号格式错误 → "请输入正确的手机号"
- 网络错误 → "保存失败，请重试"

**空状态**：无联系人时展示"还没有紧急联系人，添加后才能创建预约"，并突出"添加联系人"。

**无障碍要求**：
- 每行是单个 VoiceOver 元素，依次播报姓名、关系（有则播）、脱敏电话、是否主联系人、可用操作
- 删除为破坏性操作，必须二次确认
- 主按钮最小高度 64pt
- 增删改和设为主联系人成功后播报结果与当前联系人数量 / 主联系人
- 播报和 accessibility 值只使用脱敏电话，不读出完整号码

---

## 页面 4：盲人首页

**页面目标**：盲人跑者的功能入口，根据是否有活跃订单展示不同内容。

**入口**：登录成功 / 资料填写完成 / 服务完成返回。

**布局（2026-08-07 改）**：`ZStack` 两层 —— 地图作为背景铺满上半屏，内容面板压在其上滚动。
**视觉顺序与 VoiceOver 遍历顺序在此刻意解耦**：地图视觉上在最前，遍历顺序上排在内容之后
（`accessibilitySortPriority`）。规格允许这样做的两个前提必须同时成立：地图**不可交互**，
且**不承载任何文字里没有的信息**。设置齿轮悬浮在地图右上角，遍历顺序排到最后 ——
它此前在标题行的 `HStack` 里，是进首页遍历到的第 2 个元素。

**主要内容（无活跃订单时）**：
- 无活跃订单状态摘要，说明当前可以创建新的陪跑预约
- "开始约跑"大按钮（最小高度 64pt，醒目颜色）
- "重复当前状态"按钮（次要视觉权重，触达区仍为 64pt；**不得删除**）
- 当前定位文字描述（如"当前位置：已获取设备定位"）
- 辅助高德地图作为背景层（显示当前位置标记，VoiceOver 遍历顺序在状态、主操作和重复状态之后）
- 底部常驻"紧急呼叫"条（见下方"底部求助条"）
- 设置入口：地图右上角悬浮齿轮（~~底部操作栏~~ / ~~切换角色~~ **【未决 / 未实现】入口已删除**）

**主要内容（有活跃订单时）**：
- 订单状态卡片（覆盖"开始约跑"按钮区域）：
  - 订单状态文字（系统派单中 / 待出发 / 志愿者已到达 / 进行中）
  - 预约时间、出发地点
  - 志愿者信息（接单后显示昵称）
- 点击卡片进入对应订单详情页
- "查看当前订单"按钮
- "重复当前状态"按钮
- "取消订单"按钮（订单状态可取消时显示，包含 REMATCHING）
- 当前定位或订单出发点文字摘要
- 辅助高德地图作为背景层（显示订单地点标记，VoiceOver 遍历顺序在状态、主操作、重复状态和取消动作之后）
- 底部常驻求助条：`IN_PROGRESS` 时为"一键求助"，其余状态为"紧急呼叫"（见下）

**底部求助条（2026-08-07 新增）**：
挂在 `.safeAreaInset(edge: .bottom)`，滚动时不离开屏幕；同时响应 VoiceOver Magic Tap（双指双击）。
**模式由订单状态决定，用户不可选**：

| 订单状态 | 行为 |
|---|---|
| `IN_PROGRESS`（本人订单） | "一键求助"，走 `POST /api/emergency/trigger`，逐字锁定的二次确认 + 新鲜 GCJ-02 坐标闸门，与页面 7 完全同一条链路 |
| 其余任何状态（含无订单） | **"紧急呼叫"：纯本地拨号，绝不调任何求助端点。** 弹出选择：唯一主紧急联系人（`singlePrimary` 为 nil 时不提供）/ 110 |

降级分支的文案必须说清"App 不会代你发送求助"——按下去只有拨号音，不说清就等同于让盲人
以为求助已经发出。这条由 `EmergencySOSTests.testLocalCallCopySaysTheAppSendsNothing` 守住。

**主要操作**：
- 点击"开始约跑" → 创建预约页
- 点击订单状态卡片 → 对应订单状态页
- 点击"取消订单" → 二次确认 → `POST /api/orders/{id}/cancel`
- 点击"重复当前状态" → TTS 重新播报
- 点击底部求助条 / Magic Tap → 按上表分流

**状态变化**：
- 无订单 → 有订单：UI 从"开始约跑"切换为订单卡片
- 有订单 → 无订单：UI 恢复"开始约跑"；REMATCHING 取消成功后也返回无活跃订单状态
- 与当前订单关联且 `fromStatus` / `toStatus` 均合法的 `ORDER_STATUS_CHANGED` 先更新内存中的订单状态并立即播报，再在后台刷新完整详情；错误订单、非法状态、重复事件和与当前状态不一致的迟到事件只触发安全刷新，不直接覆盖页面
- 相同合法 `messageId` 与状态指纹的重连重发不得再次更新页面、播报或触发 REST；UUID 身份碰撞只允许一次有界安全刷新。生命周期 `APP_NOTIFICATION` 与结构化状态事件并行到达时只保留客户端固定状态文案的一次播报
- 首页与订单详情共享同一订单状态协调结果；WebSocket 已推进到 `DRIVER_EN_ROUTE` 后，较早发起的 REST 请求即使返回 `PENDING_ACCEPT` 也不得回退页面、播报或同行会话
- 状态更新完成后，定位会话、位置发送、地图 marker 和完整详情刷新分别在后续执行周期调度；任一副作用永久悬挂都不得阻塞当前状态、滚动或"重复当前状态"
- 页面生命周期、WebSocket 重连、定时和手动刷新同时触发时，合并为同一项首页请求；重复触发不得取消重启请求或推迟原 20 秒截止
- Debug、DemoRelease 和 Production 均直接挂载一张辅助高德地图；角色根路由离开时随首页卸载，不使用发布占位开关

**错误状态**：
- 定位失败 → 明确显示"使用演示坐标"或"正在获取设备定位"，不得暗示已获得真实会合点
- 网络请求加载、超时或失败不得阻断首页滚动、设置、"重复当前状态"、位置摘要或辅助地图；订单状态未确认前只阻止创建重复预约
- 首页请求超过 20 秒 → 结束加载提示，显示明确错误和可点击的"重试加载"；迟到响应不得覆盖较新的请求状态
- AMap Key 缺失或地图初始化失败 → 显示地图配置故障提示，但首页其余内容和操作保持可用
- 网络错误 → 页面仍然可用（地图显示缓存位置）

**空状态**（无活跃订单）：
- 显示无订单状态摘要 + "开始约跑"按钮 + "重复当前状态" + 辅助地图，属于正常状态，非空状态。

**无障碍要求**：
- "开始约跑"按钮：最小高度 64pt，accessibilityLabel = "开始约跑"
- 订单状态卡片：accessibilityLabel = "当前订单：" + 订单状态中文
- "取消订单"按钮：最小高度 64pt，需二次确认，accessibilityHint = "需要确认后取消当前订单"
- 辅助地图：accessibilityLabel 必须包含"辅助地图"或"辅助位置确认"，不得成为首个有意义 VoiceOver 元素；作为背景层时必须 `allowsHitTesting(false)` 且 `accessibilitySortPriority` 低于内容层
- TTS：进入页面播报"欢迎来到助盲跑"（无订单）或播报当前订单状态；复述需包含当前位置/出发地点的可理解摘要
- "重复当前状态"按钮：accessibilityLabel = "重复当前状态"
- 底部求助条：最小高度 64pt，常驻可达无需滚动，并注册 `.accessibilityAction(.magicTap)`；
  非 `IN_PROGRESS` 时 accessibilityLabel = "紧急呼叫，直接拨打电话"，hint 必须说明 App 不会代发求助
- 设置齿轮：64pt 触达区，accessibilityLabel = "设置"，遍历顺序排在地图之后（不得回到标题行）

---

## 页面 5：创建预约页

**页面目标**：盲人跑者创建跑步预约订单。

**入口**：盲人首页 → 点击"开始约跑"。

**前置条件**：角色为 `BLIND`、盲人资料完整、**已保存 1～5 个紧急联系人且恰好 1 个为主联系人**、定位权限已授予、已确认出发地点、预约时间至少在 30 分钟后。实名认证状态不属于前置条件，只作引导性提示。

**语音下单：一次说完 → 读回整单 → 确认或定点修改**
- 第 1 轮请用户用一整句说完三项："请说你想从哪儿出发、什么时候跑、跑多久，比如：明天早上八点从人民广场出发跑一个小时。"
- 这一句发**一个**请求（`POST /api/orders/voice/parse`，2026-08-04 起）同时抽起点、开始时间、时长。**这一轮不重问、不失败**：抽出多少算多少，抽不出的保留默认值（起点默认当前位置、时间默认最早可约时间）。把用户按在同一句话上反复喊，正是这里要避免的
- 第 2 轮读回整单，三段缺一不可：**先念用户原话**（否则分不清是自己说错还是系统听错）+ **再念最终要下的单**（否则不知道默认值补了什么）+ **最后给出出路**（看不见屏幕的人不会自己发现还能说"改时间"）
- 时长**说多少就是多少**，只在超出契约区间（10～300 分钟）时夹到边界，且夹了就**必须说出来**："你说的是 400 分钟，超出了可预约范围，本次按 5 小时预约。" 静默改动对听不见屏幕的人就是篡改。（就近 snap 到 6 个枚举档位已于 2026-08-06 删除 —— 枚举存在的理由是选择器需要有限选项，而说话的人不需要选择器）
- **结束地点是可选的第四项**（2026-08-09，后端 SPEC A/B1）：说了「跑到五角场」才有，没说就没有。读回时**紧跟出发地点念**，因为起终点由大模型抽取、抽反了只有读回能被发现，而两句挨着才听得出来。没说终点时一个字都不提（空 ≠ 原路返回起点）。说了地名但高德查不到坐标时照样带地名下单，并说清「这个地点没能定位到，志愿者会看到这个名字」。**表单没有终点输入框** —— 它是可选槽位，为它再加一段 POI 搜索是与起点等长的第二段交互
- **出发地撞了同名地点时，先让用户挑一个再读回**（2026-08-10，接后端 `candidates`，N48）：
  - 后端在**请求带了坐标**时用高德 POI 周边搜索 + 拼音重排取前 3 个，≥2 条即返回 `candidates`。念的是**后端的 `ttsText`**（「找到3个地点，请说第几个。第一个，五角场，杨浦区，距您800米。」）—— 名称、行政区、距离只有后端知道，客户端拼不出来
  - **必须排在读回之前**：读回念的是「我们的最佳猜测」（候选第一条），用户听完多半就说「确认」，那批候选白算了，而如果第一条不是他要的，错地点已经被确认下单 —— 而读回念得很顺，他无从分辨
  - 用户说「第二个」，序数**在本地判**（汉字与阿拉伯数字两种写法都认，`count` 是护栏：只念了 2 个不认「第三个」）
  - ⚠️ **这一轮的回答不发回 `/parse`**：「第二个」不是地名，模型会把它圈成起点 span，高德查不到又按后端口径保留下来，于是把刚挑好的地址冲掉。消歧闭环整个留在客户端
  - 挑完**同时换掉回传的槽位快照**，否则确认轮把旧 `current` 发回去，后端按旧快照继承成第一条，用户刚挑的那一下白挑且没有任何提示
  - 三次挑不出来**不丢回表单**（手上已有可用的最佳猜测，读回会念出来、仍可说「重说」），但**必须说出「没听清是第几个，先按第一个来」** —— 静默取第一条正是这批改动要消灭的那个失败
  - **上限只有 3 个**（后端截断，不是客户端展示限制）：纯听觉且要同时理解句子时，人平均只能记住约 3 项
  - **终点永远没有候选**：一次播起点 3 项 + 终点 3 项超过上面那条上限。终点抽错走读回 + 「终点错了」
- **读回之后不是「确认 or 整句重说」二选一**（2026-08-10，接后端 `current` / `userIntent` / `correctionTarget`）。用户可以：
  - **直接说要改的那一项**："把时间改成明天早上九点" → 只有时间变，起点/终点/时长/可选槽位一个都不丢，改完回到读回把整单再念一遍
  - **只点名不给值**："我想改一下终点" → 播后端的定向追问语（"好的，请说新的结束地点"），再收一次音，下一句只说值就回到读回。**不换步骤、不换屏**
  - **只说"不对"** → 播消歧问句"您想改哪一项？"
  - **要求重听 / 取消 / 整句重说** → 各自到位
- 实现是**两档**：命中本地 27 个整串词（确认 / 重说 / 重听三组）→ 零延迟直通不发网络；其余一律发 `POST /api/orders/voice/parse` 并**带上上一轮的槽位快照 `current`**，由后端分意图
  - 本地表**不撤**（后端建议撤过）：后端的确定性兜底跑在服务端，挡不住网络。一次确认往返 3.4~4.3 秒、超时 12 秒，让最高频的那句"确认"等 4 秒、网络一抖就下不了单，等于把一次已经解析成功的语音下单在最后一步丢掉
  - 本地表必须是后端 `VoiceSlotParser` 那 5 条 `INTENT_*` 正则的**子集**，由 `scripts/validate-voice-intent-words.mjs` 逐词对撞钉住。肯定词仍只整串匹配（"不确认""确认一下时间"都含"确认"但意思相反）
  - 快照**只从上一次响应派生，绝不从表单 view model 派生** —— `appointmentTime` 的初值是 `Date()`，从它取会把一个用户从没说过的时刻当成"已确认槽位"发出去
- ~~⚠️ 已知缺口：整句里说了地点但后端抽不出时，会静默落回当前位置~~ **已修（2026-08-10，N48）**：接了 `addressUnresolved`，听见了地名却没查到时，读回**前**先说一句「没找到你说的那个地点，出发地先按当前位置来，要改就说『重说』」，且与时长夹取提示**一起说**而不是二选一。措辞上说的是「没找到」不是「没听到」—— 我们听到了，是查不到
  - ⚠️ 这个 `true` 从 2026-08-10 起会**变常见**：后端同批不再拿全国范围的正向编码兜底（那条路曾把深圳说的地名解析到海南），改成诚实认输。**代价是约 50 公里外的地点会解析不出、被追问**，收益是不会把人约到另一个省
- **语音开始时主动要一次定位**（2026-08-10，N48 的根因第一环）：`onAppear` 的持续定位在非陪跑模式下 `distanceFilter = 10`，**站着不动 Core Location 不推新样本**，而语音下单恰恰是站着说完一整句（10~20 秒）。不主动要一次新 fix，请求就不带坐标 —— 而**没有坐标后端连候选都给不出来**，只能拿地名做全国范围解析。兜底是把语音这一处的样本新鲜度门放宽到 300 秒（**只这一处**，紧急求助等另外三个调用点各有各的要求）

**页面形态：语音态与表单态二选一，绝不同屏共存**（2026-08-08）
- 语音在跑时，内容区**只有一块状态区**（占满内容区，读回轮下面再加一张整单），底栏是「重复一遍」「改用表单」两个全宽竖排按钮。**四步表单、地点搜索框、补充描述框、辅助地图一个都不渲染。**
  - 状态区对 VoiceOver 是**一个**焦点：label 说现在能做什么（正在录音 / 双击跳过播报 / 正在识别），value 是系统刚念的那句话
  - 解析途中状态区仍在（否则那几秒屏幕上没有任何东西说「正在识别」），但不暴露可激活动作
  - 读回轮那张整单一行「一个名字 + 一个值」，不写「出发地点：」这类前缀；**时间只在真的抽到时才显示具体时刻**，抽不到就写「还没说」；**结束地点只在说了时才有那一行**（查不到坐标的标「（未定位到）」）
- 停语音（按「改用表单」）或语音降级时，整页换成表单态，并**停在向导刚念过的那一步**（确认步）
- 表单**不移出本页**：它既是降级出路也是隐私出路（在公共场合不愿说出自己的位置时的唯一替代路径）
- 语音**自己放弃**后（不是用户主动切换），若前置门槛全通过，表单顶部给一条**零输入下单**路径：用当前位置 + 最早可约时间成单，两步 —— 第一步念整单，第二步按钮的标题即整单（含绝对时刻，不说「最早可约时间」），旁边留「不用了，我自己填」

**主要内容（表单态）**：
- 引导步骤 1：确认出发地点
  - 当前出发地点文本摘要先于地图展示
  - 可保留默认当前位置，也可通过高德 POI 搜索选择地点
  - 搜索输入框 + 麦克风按钮（语音输入）
  - POI 搜索结果列表（显示地点名称和地址，不显示原始经纬度）
  - 辅助高德地图位于文字摘要、搜索和结果之后，只用于视觉确认
- 引导步骤 2：预约时间选择（DatePicker，不早于当前时间 + 30 分钟）
- 引导步骤 3：跑步需求（全部选填）
  - 路线备注输入框（带麦克风按钮）
  - 预计跑步时长选择（15 分钟 / 30 分钟 / 45 分钟 / 1 小时 / 1.5 小时 / 2 小时 / 不填写）
  - 配速偏好选择（走跑结合 / 轻松 / 中等 / 快速 / 无偏好）
  - 路线偏好选择（公园步道 / 街道 / 跑道 / 无偏好）
  - 本次携带导盲犬开关
  - 特殊说明输入框（选填，多行文本，带麦克风按钮）
- 引导步骤 4：确认并提交
  - 汇总出发地点、预约时间和已填写的选填需求
  - 未填写的选填字段不显示在确认摘要或复述语音中
  - "提交预约"按钮（最小高度 64pt）

**主要操作**：
- 每一步点击"下一步"进入后续步骤；不满足必填条件时显示并播报阻塞原因
- 点击"重复当前状态" → 播报当前步骤、当前值、下一步动作和阻塞错误
- 点击"提交预约" → 创建订单 → 订单状态等待页

**状态变化**：
- 定位初始化 → 自动填充当前位置
- 地点搜索 → 显示 POI 结果列表；语音识别结束且识别文本非空时自动搜索
- 步骤切换 → 只播报新步骤摘要，不在每次输入或 DatePicker 调整时自动长播报
- 提交 → loading → 成功跳转 / 失败提示

**错误状态**：
- 缺少紧急联系人或没有恰好 1 个主联系人 → 阻止提交，"请先添加紧急联系人并设置主联系人" + 跳转紧急联系人管理页入口
- 定位权限拒绝 → "需要开启定位权限才能创建预约" + "去设置"按钮
- 定位失败 → 使用默认测试坐标时明确标注"演示坐标"，不得暗示是真实出发点
- 预约时间不足 30 分钟 → "预约时间需至少在 30 分钟后"
- 出发地点为空 → "请选择出发地点"
- 网络错误 → "提交失败，请重试"

多个前置条件同时缺失时，只展示并播报**第一个可执行的**缺失项；"重复当前状态"必须包含该阻断原因。

**空状态**：
- POI 搜索结果为空 → "未搜索到相关地点"

**无障碍要求**：
- 出发地点搜索框：accessibilityLabel = "搜索出发地点"
- 麦克风按钮：accessibilityLabel = "语音输入出发地点"
- 语音识别中：搜索按钮显示并播报"语音识别中"，识别成功后自动搜索地点
- 搜索结果：VoiceOver 播报结果数量和第一个地点名称，结果项读出地点名称和地址
- 时间选择器：使用系统 DatePicker，可被 VoiceOver 访问
- 辅助地图：accessibilityLabel 必须说明"辅助位置确认"，不得朗读原始经纬度
- 下一步 / 提交按钮：最小高度 64pt
- TTS：进入页面不自动播报，提交成功后播报"订单提交成功"

---

## 页面 6：盲人订单状态等待页

**页面目标**：展示订单当前状态，等待系统派单、志愿者接单和到达。

**入口**：创建预约提交成功 / 盲人首页点击订单卡片。

**主要内容**：
- 大号状态指示器：
  - PENDING_MATCH：动画旋转圆圈 + "系统正在为你派单，请稍候"
  - PENDING_ACCEPT：志愿者卡片 + "待出发"，提示按预约时间前往或等待在出发地点
  - DRIVER_EN_ROUTE：志愿者卡片 + "志愿者已出发，正在前往出发地点"
  - DRIVER_ARRIVED：志愿者卡片 + "志愿者已到达约定地点，等待志愿者开始服务"
- 订单信息卡片（出发地点、预约时间、可选项）
- 志愿者距离（收到位置且订单有出发坐标时）：显示"志愿者距出发地点约 X"，来源为志愿者最新 WebSocket 位置到订单出发坐标
- "取消订单"按钮（PENDING_MATCH / PENDING_ACCEPT / REMATCHING 状态显示，灰色/危险色）
- 本页覆盖的状态均不显示"一键求助"入口（求助仅在 IN_PROGRESS 对盲人显示，见页面 7）
- "重复当前状态"按钮

**主要操作**：
- PENDING_MATCH / PENDING_ACCEPT / REMATCHING：点击"取消订单"（二次确认）
- 点击"重复当前状态"

**状态变化**：
- 每 5 秒轮询 GET /api/orders/{orderId}
- PENDING_MATCH → PENDING_ACCEPT：更新 UI 为"待出发" + TTS "志愿者已接单"，并朗读预约时间和出发地点，提示前往或等待在出发地点
- PENDING_MATCH → CANCELLED / NO_VOLUNTEER：显示"暂时没有可用志愿者" + TTS
- PENDING_ACCEPT / REMATCHING → CANCELLED：显示"预约已取消" + TTS
- REMATCHING → CANCELLED：后端已确认 `/api/orders/{id}/cancel` 接受该状态；iOS 仅调用现有取消接口，请求必须使用盲人 token，志愿者 token 不适用
- PENDING_ACCEPT → DRIVER_EN_ROUTE：更新状态 + TTS "志愿者已出发，正在前往出发地点"
- DRIVER_EN_ROUTE → DRIVER_ARRIVED：更新状态 + TTS "志愿者已到达"
- DRIVER_ARRIVED → IN_PROGRESS：跳转服务中页面 + TTS "服务已开始"

**错误状态**：
- 网络错误 → 保留上次查询结果，静默重试
- 轮询接口异常 → Toast "获取订单状态失败"

**空状态**：不适用（订单已创建）。

**无障碍要求**：
- 状态文本：大号字体，高对比度
- TTS：每次状态变化自动播报；PENDING_MATCH 播报"预约已提交，系统正在为你派单"
- "取消订单"按钮：二次确认弹窗 + accessibilityHint = "取消当前订单"
- 本页覆盖的状态均不显示"一键求助"或"紧急求助"按钮
- "重复当前状态"按钮：accessibilityLabel = "重复当前状态"；先播报订单状态，若存在最新求助状态则追加在后，不替代订单状态

---

## 页面 7：盲人服务中页

**页面目标**：在服务进行中显示相关信息，并提供盲人一键求助入口。

**入口**：订单状态变为 IN_PROGRESS 后自动跳转 / 盲人首页点击进行中订单。

**主要内容**：
- 页面标题："服务进行中"
- 志愿者信息卡片（昵称、联系电话 — 可点击拨打）
- 服务开始时间 / 已进行时长
- "一键求助"按钮（危险色，仅 IN_PROGRESS 且角色为盲人时显示）
- 最新求助状态文案（求助发出后显示，失败态用危险色）
- "重复当前状态"按钮

**主要操作**：
- 点击志愿者电话 → 系统拨号
- 点击"一键求助" → 二次确认："是否确认进入求助状态？确认后，本次服务将标记为异常，系统会记录当前订单状态。" → 选择"确认求助"提交，选择"取消"什么都不发送
- 点击"重复当前状态" → TTS 播报

**状态变化**：
- 每 5 秒轮询
- IN_PROGRESS → COMPLETED：跳转完成/评分页 + TTS "服务已完成"
- IN_PROGRESS：订单状态保持不变，等待志愿者结束服务或取消
- 求助被受理：`POST /api/emergency/trigger` 返回 `{success, eventId, status}`，按 `status`（PENDING / VOLUNTEER_NOTIFIED / VOLUNTEER_CONFIRMED / CS_HANDLING / CONTACT_NOTIFIED / RESOLVED / FALSE_ALARM）显示并播报进行时文案；**订单状态不变**
- 求助后续进展由实时通知驱动；状态存放在 app 生命周期的 `EmergencyCoordinator`，离开本页再回来仍在，不落盘

**错误状态**：
- 网络错误 → 保留当前 UI，静默重试
- 拨号失败 → "无法拨打电话"
- 无新鲜真实 GCJ-02 坐标 → **不发送请求**，显示并播报"求助未发出：当前无法获取你的位置。请在设置中允许定位后重试，或直接拨打110。"
- 冷却期 → HTTP 429 `TOO_MANY_REQUESTS`，按响应 `retryAfterSeconds` 提示稍后再试
- 非订单参与方 → 403 `NOT_ORDER_PARTICIPANT`；订单不存在 → 400 `BAD_REQUEST`；两者均以"求助未发出"开头如实说明并指向 110

**空状态**：不适用。

**无障碍要求**：
- 志愿者电话：accessibilityLabel = "拨打志愿者电话 " + 电话号码
- "一键求助"按钮：最小高度 64pt，accessibilityLabel = "一键求助，遇到紧急情况时点击"，accessibilityHint 说明需要二次确认并会上报当前位置；每次求助状态变化都同时更新可见文案和 TTS
- 求助文案不得声称短信已送达或紧急联系人已被联系上；`EMERGENCY_CONTACT_NOTIFIED` 一律呈现为"系统正在联系你的紧急联系人，尚未确认对方是否收到。若情况危急请立即拨打110。"
- TTS：进入页面播报"服务已开始，祝您跑步愉快"
- "重复当前状态"按钮：accessibilityLabel = "重复当前状态"；先播报订单状态，再追加最新求助状态，不替代订单状态

---

## 页面 8：盲人完成/评分页

**页面目标**：显示服务完成状态，提供评分入口。

**入口**：订单状态变为 COMPLETED 后自动跳转。

**主要内容**：
- 完成图标 / 动画
- 服务摘要（时长、志愿者昵称、出发地点）
- 星级评分（1-5 星，可点击选择）
- 文字反馈输入框（选填，多行，带麦克风按钮）
- "提交评价"按钮
- "跳过" / "返回首页"链接

**主要操作**：
- 选择星级
- 填写文字反馈
- 点击"提交评价" → 提交成功 → 返回首页
- 点击"跳过" → 直接返回首页

**状态变化**：
- 提交评分 → loading → 成功提示 → 返回首页
- 跳过评分 → 返回首页

**错误状态**：
- 评分提交失败 → Toast "提交失败，可稍后在服务记录中评价"

**空状态**：不适用。

**无障碍要求**：
- TTS：进入页面播报"服务已完成"
- 星级评分：accessibilityLabel = "第 N 颗星"，可逐颗选择
- 文字反馈：麦克风按钮可用
- "提交评价"按钮：最小高度 64pt
- "跳过"按钮：accessibilityLabel = "跳过评价，返回首页"

---

## 页面 9：志愿者资料认证页

**页面目标**：志愿者通过基本信息与身份证二要素核验、动作活体认证直接完成主注册流程。

**入口**：角色选择页选择"志愿者"（首次）/ 设置页 → 编辑资料。

**主要内容**：
- 标题："志愿者认证"
- 昵称输入框（必填）
- 手机号显示（自动填充登录手机号）
  - 认证区域：
  - 提示文字："请完成以下认证步骤（Demo 版为模拟认证）"
  - 基本信息与身份证二要素、动作活体认证入口
  - 两步进度显示：基本信息与身份核验、活体认证
  - 主注册状态显示（STEP_1_BASIC_INFO / STEP_3_FACE_VERIFY / STEP_4_COMPLETED；STEP_4_TRAINING 仅为遗留兼容状态）
- "提交"按钮

**主要操作**：
- 填写昵称
- 点击开始认证 → 进入志愿者注册流程 → 填写基本信息、与身份证一致的姓名和身份证号码 → 后端完成身份证二要素核验 → init 获取 certifyId → iOS 通过 AliyunFaceAuthFacade 原生 SDK 完成动作活体 → SDK 回调后轮询 result → 后端直接完成注册
- 点击"提交" → 保存资料 → 进入志愿者首页

**状态变化**：
- 身份证二要素核验不通过：停留 STEP_1_BASIC_INFO，显示后端错误并允许更新身份证信息
- 身份证二要素核验通过：currentStep = STEP_3_FACE_VERIFY，进入动作活体认证
- 动作活体认证 init 返回 PENDING 和非空 certifyId：iOS 向原生 SDK 传入 certifyId 和当前 ViewController，用户按 SDK 页面提示完成动作后前端轮询 result；原生流程不要求 certifyUrl
- 活体认证通过：后端原子设置 currentStep = STEP_4_COMPLETED、canAcceptOrders = true；新注册不得进入 STEP_4_TRAINING
- 活体 result 已通过但 status 尚未完成：显示“活体已通过，注册状态同步中，无需课程或答题”，每 5 秒轮询并提供手动刷新，不允许重复发起活体认证
- status 返回 STEP_4_COMPLETED、遗留 STEP_4_TRAINING 或 canAcceptOrders = true：显示并播报“注册完成，请返回首页开启可服务状态”
- STEP_4_TRAINING 仅用于旧账号或尚未升级后端的兼容；iOS 将其按主注册完成处理并停止轮询，但不会自动开启可服务状态
- 可选资质证书上传使用 `POST /api/volunteer/verification`（multipart），不属于主注册引导链路，不影响接单资格。约束：单文件 ≤ 5 MB；扩展名白名单 `.jpg` / `.jpeg` / `.png` / `.gif` / `.webp` / `.bmp` / `.pdf`，后端同时校验文件魔数，扩展名与内容必须一致。**白名单没有 `.heic`**，相册里的 HEIC 必须由 iOS 先转成 JPEG 再上传，否则一定 400。状态查询 `GET /api/volunteer/verification/status` → `{"status":"NONE|PENDING|APPROVED|REJECTED"}`；状态查询失败时不猜测状态、也不允许盲传
- 可服务开关由志愿者首页单独控制，认证不自动开启 isAvailable / wantsDispatch
- 提交：loading → 成功跳转

**错误状态**：
- 昵称为空 → "请填写昵称"
- 未完成主注册流程 → "请先完成志愿者注册流程"
- 身份证二要素失败 → 显示后端失败原因并允许重新提交 Step1
- 活体认证发起时返回身份信息错误 → 允许返回 Step1 修改姓名和身份证号码后重新提交
- 原生 SDK 用户取消 → 留在活体认证步骤，清除本次 certifyId 并允许重新发起
- 原生 SDK 系统、网络或设备时间错误 → 显示对应原因，清除本次 certifyId 并允许重新发起
- 原生 SDK 返回 1001 → 仅展示根据白名单 subcode 映射的提示和安全错误码；不得展示原始 SDK message、certifyId、metaInfo 或身份字段
- Z1014/Z1023 → SDK 初始化或内部处理失败；I4001 → 刷脸模块接入异常；Z1010/Z1037 → 认证参数或流程配置异常；Z1001/Z1002/Z1020 → 相机不可用；Z1024 → 流程重复发起
- 活体认证拒绝 → 显示 faceVerifyRejectionReason 或 result.message，并允许重新发起
- 活体认证通过但 status 仍未进入 STEP_4_TRAINING / STEP_4_COMPLETED 且 canAcceptOrders 不为 true → 保持同步等待，禁止课程/答题入口和重复活体认证
- 网络错误 → "保存失败"

**空状态**：无（首次使用时为空字段）。

**无障碍要求**：
- 昵称输入框：accessibilityLabel = "请输入昵称"
- 认证按钮：accessibilityLabel = "开始认证"
- 注册完成结果：accessibilityLabel = "注册完成，请返回首页开启可服务状态"
- 提交按钮：最小高度 64pt

---

## 页面 10：志愿者首页

**页面目标**：志愿者系统派单工作台，查看可接单状态、覆盖范围、当前订单和服务统计。

**入口**：认证完成 / 登录（已有认证）/ 服务完成返回。

**主要内容**：
- 用户信息区（昵称、系统派单状态、定位摘要），固定覆盖在地图顶部安全区下方，紧贴灵动岛、刘海或状态栏下沿
- 可服务开关（Toggle，新志愿者默认关闭，由用户手动开启；seed demo 账号可预置开启）
- 高德地图全屏背景（显示当前位置和覆盖半径；隐藏指南针）
- 派单状态卡片：
  - `canDispatch`、`notAvailableReasons`、`isOnline`、`isWithinServiceTime`
  - 覆盖半径 `coverageRadiusKm`
- 服务统计卡片：
  - 积分占位、完成次数、平均评分、派单/接受/拒绝/超时次数、接单率
- 当前订单提示框（仅有志愿者活跃订单时显示）：
  - 状态、盲人昵称、出发地点、预约时间
  - PENDING_ACCEPT / DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS 点击进入服务流程页面
- 近期订单列表：
  - 每个卡片：盲人昵称、出发地点、预约时间、状态、评分、积分占位
- 底部标签栏：
  - 首页
  - 服务记录
  - 积分商城
  - 设置

**主要操作**：
- 切换可服务开关
- 点击当前订单提示框 → 当前订单详情或服务页面；活跃服务状态直接进入服务流程页面
- 点击近期订单卡片 → 查看记录详情（如当前订单则进入当前订单）
- ~~点击切换角色 → 角色切换（检查活跃订单）~~ **【未决 / 未实现】入口已删除**

**状态变化**：
- 可服务开关切换：即时更新（API 调用）
- 当前订单、近期订单和统计：进入首页、返回首页以及首页保持前台期间每 10 秒通过 `/api/volunteer/dispatch-summary` 刷新；首次位置上报或可服务开关更新后延迟 1 秒重查；WebSocket 重连成功则立即补发最新真实位置，再刷新摘要
- 与当前订单关联且状态枚举、原状态均匹配的 `ORDER_STATUS_CHANGED` 立即更新当前订单摘要，完整派单摘要继续在后台刷新；非法、错误订单、重复或迟到事件不得直接改写本地订单
- 状态消息按 UUID 与订单/状态指纹在当前登录会话内去重；重连重发不重复更新、播报或刷新，身份碰撞不采用消息内容并只触发一次有界安全刷新
- 首页、订单详情和服务页共享同一订单状态协调结果；重复页面订阅不得重复启动同行定位会话，迟到的旧 REST 摘要不得将已确认状态回退
- 页面生命周期、WebSocket 重连、定时和手动刷新命中同一首页端点时只保留一个请求和原始 20 秒截止，不得通过取消重启无限延后超时
- 外部地图跳转、锁屏、切后台或页面切换取消刷新任务时，必须立即结束加载提示；正常任务取消不得显示为网络错误，回到前台后可再次刷新
- Debug、DemoRelease 和 Production 均直接挂载一张全屏高德地图；刷新只更新标记和视口数据，不得用条件分支替换根地图
- 顶部状态区不得通过 `PreferenceKey` 把子视图实时测量值反写进同一面板布局；面板上边界使用安全区和“是否存在当前订单”计算的稳定预留高度，避免两组布局高度间振荡形成 AttributeGraph 高 CPU 循环
- `MAMapView` 必须嵌入尺寸稳定的 UIKit 宿主容器，地图内部逐帧布局不得作为 `UIViewRepresentable` 根视图向 SwiftUI 传播失效；其 UIKit 内部无障碍子树对 SwiftUI 隐藏，由桥接层提供一个稳定的地图摘要元素。当前位置更新时间和派单覆盖文案由顶部状态区朗读，不得在地图逐帧渲染期间重复计算动态无障碍标签
- 地图渲染采用可降帧的低帧率上限并运行在默认 RunLoop mode；父级滚动手势开始后地图渲染必须让出主线程。本产品首页地图不提供逐帧转向导航，不得以 60 fps 空闲渲染占用首页交互预算
- Core Location 在坐标未变化时交付的新时间戳只更新网络上报样本，不得发布到首页环境树；只有坐标、权限或错误状态实际变化时才触发页面重算
- `@Published` 当前值事件用于全局通知或同行健康播报时，必须跨 SwiftUI 订阅重建保留已处理身份；TTS 自身的 `@Published` 状态变化不得使相同通知/健康状态被立即重放并形成“播报 → 根视图重算 → 重订阅”的循环
- 可服务开关开启后，只有 WebSocket 在线且已上报位置的志愿者才能收到 `NEW_ORDER`
- 地图当前位置：显示当前定位和 `coverageRadiusKm` 对应覆盖范围

**错误状态**：
- 定位权限拒绝 → 提示"需要开启定位权限才能接收系统派单"
- 已开启接单但没有可用真实位置 → 可见并朗读“定位暂不可用，可能无法收到派单”
- 派单摘要加载失败 → "加载失败，请重试"
- 派单摘要加载、超时或失败不得阻断面板拖动/滚动、刷新、回到当前位置、服务记录、积分商城或设置；已有摘要必须继续显示
- 首页请求超过 20 秒 → 结束加载提示并提供可点击的重试；不响应取消的请求和迟到响应不得恢复旧 loading 或覆盖新摘要
- AMap Key 缺失或地图初始化失败 → 显示地图配置故障提示，但顶部状态、派单面板、底栏和可服务开关保持可用
- 后端返回 `canDispatch = false` 但未返回 `notAvailableReasons` → 显示“服务端未返回不可接单原因”，不得由客户端推断或伪造接单资格
- 网络错误 → 保留上次缓存数据

**空状态**：
- 无当前订单 → "已准备好后等待系统派单"或根据不可接单原因显示说明
- 可服务开关关闭 → 显示"已关闭接单，不会收到新的系统派单"

**无障碍要求**：
- 可服务开关：accessibilityLabel = "可服务开关"，accessibilityHint = "关闭后不会收到新的系统派单"
- 当前订单提示框：accessibilityLabel = "当前订单：" + 状态中文 + 盲人昵称 + 地点，accessibilityHint = "点击进入当前订单"
- 派单状态卡片：accessibilityLabel = 当前派单状态 + 不可接单原因
- 近期订单卡片：accessibilityLabel = 盲人昵称 + 地点 + 时间 + 状态 + 积分
- TTS：进入页面播报"欢迎来到志愿者中心。当前状态：" + 派单状态文案

---

## 页面 11：志愿者订单列表页（已删除，2026-08-07）

**现状**：这一页**不存在了**，代码已从 `blindRun/Volunteer/VolunteerOrderFlowViews.swift` 删除。
不要按下面的历史记录重新实现它。

**为什么删**：

1. **它从来没有入口。** 系统派单（`GET /api/volunteer/dispatch-summary`）上线后，
   `ContentView` 志愿者侧只路由到 `VolunteerProfileView` / `VolunteerHomeView`，
   这一页唯一的构造点是 `#if DEBUG` 的 `#Preview`。
   这是有意的：见归档决策 `openspec/changes/archive/2026-07-02-enforce-formal-dispatch-service-flow/design.md` 第 7 条，
   以及活规格 `openspec/specs/system-dispatch-flow/spec.md` 的
   「Public order pool is removed from primary home flow」。
2. **它拿到的数据从 2026-05-24 起就是错的。** 它把 `GET /api/orders/available` 解成
   `PagedOrderResponse`，而后端返的是 `AvailableOrderResponse` 裸数组（无 `status` 字段），
   解码必失败；当时 `PagedOrderResponse` 的宽容解码器把失败吞成空数组且不报错。
   也就是说即使有人打开这一页，看到的也永远是「暂无可用订单」。
3. **点进详情必然 403。** 卡片跳 `VolunteerOrderDetailView` → `GET /api/orders/{id}`，
   而订单在 `PENDING_MATCH` 时后端对非参与方返 403（`OrderQueryService.java:36-39`）。

一个没有入口、拿不到数据、点进去必然失败的页面，留着只会让人以为它能用。

**历史记录（删除前的规格，仅供考古）**：标题「可接订单（非主流程）」；
按距离升序的订单列表（盲人昵称 / 出发地点 / 距离 / 预约时间）；顶部可服务开关；
下拉刷新；空态「暂无可用订单」。

**若未来要重做**：`GET /api/orders/available` 的响应见后端 `api_spec.yaml` 的
`components.schemas.AvailableOrderResponse` —— 9 个分量，**不含**坐标、盲人姓名与 `status`，
自带 `distanceKm` 且后端已按距离升序返回（客户端不需要再排一次）。
接单前拿不到订单详情，所以卡片上不能有「查看详情」，只能直接给接单动作。

---

## 页面 12：志愿者订单详情页

**页面目标**：展示订单完整信息；系统派单主流程不再停留在此页，接受成功后直接进入志愿者服务中页。

**入口**：旧版订单列表或调试/兼容路径。30 秒派单弹窗接受成功和当前活跃订单入口直接进入志愿者服务中页。

**主要内容（派单弹窗/调试接单前）**：
- 订单基本信息（出发地点、预约时间、路线备注（如有）、预计时长（如有）、配速偏好（如有）、路线偏好（如有）、导盲犬（如有）、特殊说明（如有））
- 盲人非敏感信息（昵称，**不含**联系电话）
- 高德地图：派单弹窗提供小地图预览；当前位置可用时显示绿色当前位置标记和红色出发地点标记，当前位置不可用时只显示出发地点
- 系统派单弹窗显示"接受" / "拒绝"和倒计时；详情页不作为主接单入口

**主要内容（接单后）**：
- 订单信息同上
- 盲人完整信息（昵称 + 联系电话，可点击拨打）
- 接单后主流程自动进入志愿者服务中页，不停留在"订单详情 + 进入服务页面"中间页
- 调试/兼容路径若从详情页接单成功，也应自动进入志愿者服务中页

**主要操作**：
- 派单弹窗：点击"接受" → `POST /api/orders/{id}/respond` with `action=ACCEPT`
- 派单弹窗：点击"拒绝"或超时 → `POST /api/orders/{id}/respond` with `action=DECLINE`
- 接单成功：刷新订单详情和派单摘要，直接进入志愿者服务中页
- 服务中页：点击"我已出发" → 订单变为 DRIVER_EN_ROUTE
- 服务中页：点击"我已到达" → 订单变为 DRIVER_ARRIVED
- 服务中页：PENDING_ACCEPT / DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS 显示"取消订单"；点击后二次确认 → REMATCHING，并退出志愿者服务流程

**状态变化**：
- 接受系统派单：PENDING_MATCH → PENDING_ACCEPT
- 出发：PENDING_ACCEPT → DRIVER_EN_ROUTE
- 到达：DRIVER_EN_ROUTE → DRIVER_ARRIVED
- 取消：PENDING_ACCEPT / DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS → REMATCHING；志愿者端清空本地当前订单并退出服务流程
- 志愿者端全状态不显示求助入口；求助只对盲人端在 IN_PROGRESS 开放
- 若云端接单后直接返回 `IN_PROGRESS`，iOS 保留真实状态并记录为真机联调问题；若看到 `REMATCHING`，优先排查是否志愿者接单后主动取消

**错误状态**：
- 接受派单失败（已被接 / 派单不匹配） → "该订单已无法接受" → 返回首页
- 接单失败（网络） → "接单失败，请重试"
- 地图显示失败 → "无法显示地图，请重试"

**空状态**：不适用（订单存在才有详情页）。

**无障碍要求**：
- 派单弹窗按钮：最小高度 64pt，accessibilityLabel = "接受派单" / "拒绝派单"
- "我已到达"按钮：最小高度 64pt，accessibilityLabel = "我已到达约定地点"
- "查看地图"按钮：accessibilityLabel = "查看出发点位置"
- 志愿者端全状态不显示求助入口

---

## 页面 13：志愿者服务中页

**页面目标**：志愿者从接单后前往出发地点、标记出发/到达、开始服务并完成服务的管理页面。

**入口**：订单状态变为 PENDING_ACCEPT / DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS 后进入。

**主要内容**：
- 订单状态指示
- 盲人信息卡片（昵称 + 联系电话，可拨打）
- 订单信息（出发地点、预约时间）
- 操作按钮：
  - PENDING_ACCEPT：显示"前往出发地点"，提供"导航到出发地点"、"我已出发"和"取消订单"按钮
  - DRIVER_EN_ROUTE：显示"前往出发地点"，提供"导航到出发地点"、"我已到达"和"取消订单"按钮
  - DRIVER_ARRIVED：显示已到达状态、"开始服务"按钮和"取消订单"按钮，不显示"结束服务"按钮
  - IN_PROGRESS："结束服务"按钮（最小 64pt）和"取消订单"按钮
  - PENDING_ACCEPT："取消订单"按钮
- 志愿者端仅在 `IN_PROGRESS` 显示"一键求助"入口（`RunOrderStatus.canVolunteerTriggerEmergency == (self == .inProgress)`），**自 2026-07-31 起已开放**。~~此前全状态隐藏，因为后端按触发人建事件~~ —— 后端 commit `a5ba523`（SOS-1）已把 `event.userId` 改为取订单的盲人方，用 `TriggerType.VOLUNTEER_BUTTON` 区分来源。志愿者**没有撤销按钮**（后端 403 `EMERGENCY_VOLUNTEER_CANNOT_DISMISS`）：一对一陪跑里志愿者可能就是威胁来源
- 高德地图背景：服务流以红色出发地点为唯一自定义主标记，地图中心固定在出发地点；当前位置使用高德系统蓝点辅助显示，不作为自定义 marker，不触发地图中心随位置上报重算；相同 id 的 marker 更新坐标和标题时不移除重加，不重复 drop 动画
- 高德地图背景和盲人辅助地图只在 marker 内容实际变化时更新 annotation；作为滚动内容或背景时不得截获垂直拖动手势
- 地图桥接以稳定 UIKit 宿主容器隔离 `MAMapView` 的内部布局和持续变化的无障碍元素；保留一个稳定、无原始坐标的地图摘要元素，同行角色与新鲜度继续由相邻文本区域说明
- 当前通知与同行健康状态的 TTS 只在通知 UUID 或健康枚举真正变化时触发；SwiftUI 重建 Combine 订阅后收到的相同当前值不得再次播报
- 同行 marker 的 15 秒过期由 ViewModel 在收到合法新样本时安排一次可取消任务；不得使用以动态 `.now` 创建的周期 `TimelineView` 驱动整页或地图重算。新样本替换旧任务，页面离开、订单结束或切换订单立即取消任务并清除 marker
- 外部地图导航选择器：高德地图、百度地图、苹果地图，默认步行导航；未安装的第三方地图不显示

**主要操作**：
- PENDING_ACCEPT / DRIVER_EN_ROUTE：点击"导航到出发地点" → 选择外部地图 App 并打开步行导航
- PENDING_ACCEPT：点击"我已出发" → 订单变为 DRIVER_EN_ROUTE
- DRIVER_EN_ROUTE：点击"我已到达" → 订单变为 DRIVER_ARRIVED
- DRIVER_ARRIVED：点击"开始服务" → 调用 `POST /api/orders/{id}/start-service` → 订单变为 IN_PROGRESS
- PENDING_ACCEPT / DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS：点击"取消订单" → 二次确认 → REMATCHING，志愿者端退出当前服务流程
- IN_PROGRESS：点击"结束服务" → 确认弹窗 → 服务完成
- 点击盲人电话 → 系统拨号

**状态变化**：
- 每 5 秒轮询
- PENDING_ACCEPT → DRIVER_EN_ROUTE（志愿者点击已出发）：UI 更新
- DRIVER_EN_ROUTE → DRIVER_ARRIVED（志愿者点击已到达）：UI 更新
- PENDING_ACCEPT / DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS → REMATCHING：跳转首页并提示系统将为盲人重新匹配
- 志愿者取消成功后不再用志愿者 token 拉取已解除参与关系的订单详情，直接退出服务流并清空本地当前订单
- DRIVER_ARRIVED → IN_PROGRESS（志愿者点击开始服务）：UI 更新；只有进入 IN_PROGRESS 后才显示"结束服务"
- IN_PROGRESS → COMPLETED：跳转首页 + 显示"服务完成，获得 +100 积分"
- DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS：志愿者端不显示求助入口；盲人在 IN_PROGRESS 发起求助也不改变订单状态
- `REMATCHING` 作为真实后端状态保留，表示志愿者接单后主动取消；盲人端提示"正在确认志愿者状态，请稍候；如需更换志愿者，系统会继续处理。"，并显示"取消订单"逃生按钮，取消请求使用盲人 token

**错误状态**：
- 网络错误 → 保留当前 UI，静默重试
- "我已出发"、"我已到达"、"开始服务"、"结束服务"和取消的状态变更 POST 最多等待 12 秒，并与后续详情确认分离；POST 返回后立即释放操作 spinner，不等待 GET 才恢复页面
- POST 成功或传输结果不确定时显示"操作已提交，状态待确认"，禁止重复提交同一状态转换；地图、导航、返回、取消入口和服务信息滚动保持可用
- "我已到达"只能禁用其对应的状态提交按钮；POST 或 20 秒确认查询悬挂时不得禁用服务面板滚动、返回和外部导航，也不得通过地图或同行位置过期刷新维持 spinner
- WebSocket 状态事件或带 20 秒硬截止的详情查询负责确认正式状态；仍未确认时显示"状态确认延迟"和只读"重新确认状态"，不得恢复操作 spinner，也不得重复 POST
- 明确 4xx/5xx 时恢复原状态按钮并允许重试；401 使用现有登录失效流程。迟到响应不得直接覆盖已确认的新状态
- 合法 WebSocket 状态先提交页面、按钮与播报；定位准备、位置发送、地图更新和详情刷新随后独立执行。地图渲染、地图配置故障降级和位置上报不得参与订单按钮或详情确认状态机
- 点击状态按钮时只禁用对应转换操作；ScrollView、返回、外部导航、取消入口和根视图命中始终保持可用
- 结束服务失败 → "操作失败，请重试"；若仍处于 DRIVER_ARRIVED，显示"开始服务"按钮并阻止调用 `/api/orders/{id}/finish`

**空状态**：不适用。

**无障碍要求**：
- "我已到达"按钮：最小高度 64pt，accessibilityLabel = "我已到达约定地点"
- "开始服务"按钮：最小高度 64pt，accessibilityLabel = "开始服务"，accessibilityHint = "点击后通知盲人服务已开始"
- "取消订单"按钮：PENDING_ACCEPT / DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS 显示，需二次确认，accessibilityHint = "需要确认后取消当前订单"
- "结束服务"按钮：最小高度 64pt，需二次确认
- 志愿者端全状态不显示求助入口
- TTS：进入 DRIVER_ARRIVED 状态不自动播报（由盲人端播报）
- TTS：进入 IN_PROGRESS 状态播报"服务已开始"

---

## 页面 14：志愿者服务记录页

**页面目标**：展示志愿者的历史服务记录。

**入口**：志愿者首页底部标签栏 → "服务记录"。

**主要内容**：
- 标题："服务记录"
- 订单列表（按时间倒序）：
  - 每项：服务时间、盲人昵称、出发地点、服务状态、获得积分（如有）
- 状态标签：已完成（绿色）、已取消（灰色）

**主要操作**：
- 点击记录 → 订单详情（只读模式）
- 下拉刷新 → 重新加载

**状态变化**：
- 进入页面：加载历史订单
- 下拉刷新：更新列表

**错误状态**：
- 网络错误 → "加载失败，下拉重试"

**空状态**：
- 无服务记录 → 空状态插图 + "暂无服务记录" + "开启可服务后，系统会自动派单。"

**无障碍要求**：
- 每条记录：accessibilityLabel = "时间：" + 时间 + "，盲人：" + 昵称 + "，地点：" + 出发地点 + "，状态：" + 状态 + "，积分：" + 积分

---

## 页面 15：志愿者积分/商城占位页

**页面目标**：展示积分余额和积分商城占位信息。

**入口**：志愿者首页底部标签栏 → "积分商城"。

**主要内容**：
- 积分余额大数字显示（如 "0 积分"）
- 积分获取规则简要说明："每完成一次服务 +100 积分"
- "积分商城即将上线，敬请期待" 占位文字 + 插图
- 几个假商品卡片（占位，显示"敬请期待"）
  - 如：运动腰包、运动水壶、运动毛巾（仅为展示效果）

**主要操作**：
- 无实际可操作项（纯展示页面）

**状态变化**：
- 积分余额随服务完成更新

**错误状态**：
- 积分余额加载失败 → 显示"--"

**空状态**：不适用（积分余额始终显示，只是可能为 0）。

**无障碍要求**：
- 积分余额：accessibilityLabel = "当前积分：" + 数字 + "分"
- 商品卡片：accessibilityLabel = 商品名 + "，敬请期待"

---

## 页面 16：设置页

**页面目标**：提供个人资料编辑、关于信息、退出登录入口。

**入口**：盲人首页 / 志愿者首页 → 设置入口。

**主要内容**：
- 标题："设置"
- 用户基本信息展示区（昵称、手机号、当前角色）
- 菜单列表：
  - 个人资料 → 进入对应角色资料编辑页
  - 紧急联系人 → 仅盲人角色显示，进入紧急联系人管理页（页面 3b）
  - 实名认证 → 仅盲人角色显示，展示 `NOT_VERIFIED` / `VERIFIED` / `FAILED` 状态与提交入口。**这是引导流「稍后再说」之后唯一能回到实名页的入口**，必须常驻；实名未通过时无法创建预约
  - ~~切换角色 → 角色选择页（检查活跃订单）~~ **【未决 / 未实现】入口已删除**：后端没有角色切换端点，此菜单项已从设置页移除
  - API 环境 → 仅 Debug 开发包显示环境切换（Mock / 本地 / 演示云端）
  - 关于 → App 版本信息 + **法律条款入口（隐私政策 / 用户协议）**
  - 退出登录 → 二次确认 → 清除 token → 登录页

**法律条款入口（关于页内，盲人端与志愿者端共用）**
- **App Store 审核 5.1.1 / 5.1.2 的阻塞项**：审核员必须能在应用内找到隐私政策，且他们是**未登录**状态 —— 所以 `GET /api/misc/legal-links` 必须免鉴权请求（后端对该端点 permitAll）
- 进入关于页时拉一次，**只拉一次**（两个 URL 来自后端配置，一次会话内不变）；失败**静默**，不弹错误
- 两条落点，都保证「可点且有内容」：后端下发合法 http(s) 地址 → 外链在浏览器打开；未配置 / 请求失败 / 地址非法 → 应用内置文案页（`LegalFallbackCopy`）
- **绝不出现空白页、报错弹窗或禁用的灰按钮** —— 这三种都会让审核直接被拒
- 内置文案页每条要点单独成行，VoiceOver 可逐条浏览；分节标题带 `.isHeader` 特性

**主要操作**：
- 点击各菜单项跳转
- 退出登录 → 确认弹窗

**状态变化**：
- 退出登录：清除 Keychain token → 登录页
- ~~切换角色：检查活跃订单 → 拦截或放行~~ **【未决 / 未实现】入口已删除**
- 切换 API 环境：仅 Debug 开发包可用，即时生效，下次请求使用新环境；Demo/Production 包无入口

**错误状态**：
- 退出登录失败（罕见） → "退出失败，请重试"

**空状态**：无。

**无障碍要求**：
- 菜单列表项：标准 List 无障碍支持
- 退出登录：accessibilityHint = "退出当前账号，需要重新登录"，二次确认
- ~~切换角色：如果被拦截，弹窗有明确说明~~ **【未决 / 未实现】入口已删除**
## 设置页：退出与账户删除

- 盲人跑者和志愿者设置页均提供“退出登录”和“删除账户”。
- 退出登录保留既有二次确认；请求中禁用重复提交。撤销失败时显示并朗读原因，允许重试，或再次确认“仅退出本机”，且明确远端 Token 可能仍有效。
- 删除账户使用两阶段破坏性确认，说明历史数据软删除、全部 Token 失效及手机号可重新注册；不存在“仅本机删除”。
- 阻断状态为 `PENDING_MATCH`、`PENDING_ACCEPT`、`DRIVER_EN_ROUTE`、`DRIVER_ARRIVED`、`IN_PROGRESS`、`REMATCHING`。

## 全局前台实时通知层

**页面归属**：位于 `ContentView` 根层级，覆盖盲人/志愿者导航但不归属某个功能页。

**显示规则**：

- 显示可选标题、安全正文、优先级图标和“关闭”按钮；不展示原始 payload、坐标或内部 event key。
- `HIGH` 立即抢占正在显示的 `NORMAL`，被抢占普通通知回到最多 24 条的内存队列；同优先级按到达顺序展示。队列溢出时依次淘汰最早的 `NORMAL`、非安全 `HIGH`，仅当队列全部为安全事件时淘汰最早安全事件，以优先保留较新的安全提醒。
- 可见正文、合并后的 VoiceOver label 和 TTS 使用同一安全语义；关闭按钮提示会继续显示下一条。
- 活动订单的生命周期模板通知不直接展示/播报，由订单详情刷新后的本地状态文案替代。
- 派单 overlay 保持志愿者现有接受/拒绝和倒计时 UI，但数据源改为 app-lifetime retained prompt，导航不得重置后端截止时间。

**边界**：本层不提供分离操作、紧急操作、通知收件箱或后台推送。peer marker 仅按 `enable-live-escort-location-and-track-summary` 向当前订单双方开放。

## 实时同行位置组件

- 盲人订单状态页在状态、主要操作和“重复当前状态”之后展示志愿者辅助 marker；志愿者服务地图展示盲人辅助 marker。
- 仅接受匹配当前订单、角色方向正确、合法且不超过 15 秒的位置。过期后移除 marker，并显示“同行位置暂时不可用”。
- 每个角色页面至多保留一个同行位置过期任务；新样本取消旧任务，旧任务、错误订单和迟到样本不得清除更新后的 marker。页面离开、订单结束或切换订单时立即取消并清理。
- 禁止用动态 `.now` 的周期时间线检查新鲜度；无新位置、状态或用户交互时，同行位置组件不得周期性触发 SwiftUI/AttributeGraph 更新。
- 高频同行位置按订单与角色合并，每个主线程提交周期只交付最新样本；状态和通知事件保持可靠顺序，不因位置合并而丢弃。
- 辅助/背景地图不得拦截页面的垂直滚动手势，annotation 内容未变化时不得触发移除重加。
- 页面不得显示或朗读原始经纬度。地图 accessibilityLabel 只说明同行角色与位置新鲜度。
- `IN_PROGRESS` 显示“陪跑期间将持续使用定位，锁屏或后台仍会记录；服务结束后自动停止”，并显示权限、位置过期、网络间断和后台记录状态。

## 完成轨迹总结组件

- 标题固定为“本次路线”，只绘制 `blindTrack`；地图位于状态和文字统计之后。
- 统计按可用字段展示“里程、时长、平均配速”；缺失字段显示“暂无数据”，不得计算或填充 Demo 值。
- 空轨迹结合响应 `status` 显示尚未开始、仍在采集或历史订单暂无轨迹。
- 盲人完成/评价页、志愿者完成页和服务记录详情复用同一组件，并提供 VoiceOver、TTS 和“重复当前状态”。
- `volunteerTrack` 不作为第二路线展示，不输出“异常”“偏离”等结论。
